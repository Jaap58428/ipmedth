// KARMA INSTRUCTIONS
// run 'npm install' in command line from root project folder to setup karma framework
// start tests (and keep them running on changes) with 'karma start'
// below you can find an example test code format, use this in your own file of choise
// put this file in the /test/ folder only! 

describe("tryTestSuite", function () {

    it("compares true to true", function () {
        // Invoke the unit being tested as necessary

        // Set up the pre-test
        // grades.push(new Grade("Jaap", 3.8));
        // grades.push(new Grade("Fred", 9.1));
        // grades.push(new Grade("Jeroen", 5.6));
        // var gr = getHighestGrade();

        // Check the results; "expect" and toEqual are Jasmine methods.
        expect(true).toEqual(true);
    });
})